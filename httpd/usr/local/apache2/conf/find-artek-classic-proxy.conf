<VirtualHost *:80>
    ServerName find-artek-classic.vezit.net
    ServerAdmin victor.reipur@gmail.com

    # Serve static files
    Alias /static/ /app-data/staticfiles-1-6-11/
    <Directory /app-data/staticfiles-1-6-11/>
        Require all granted
    </Directory>

    # Serve media files
    Alias /media/ /app-data/media/
    <Directory /app-data/media/>
        Require all granted
    </Directory>

    # Serve report files
    Alias /reports/ /app-data/media/reports/
    <Directory /app-data/media/reports/>
        Require all granted
    </Directory>

    # Configure mod_proxy to forward requests to the Django container
    ProxyPreserveHost On
    ProxyPass /static !
    ProxyPass /reports !
    ProxyPass /media !
    ProxyPass / http://find-artek-classic:80/
    ProxyPassReverse / http://find-artek-classic:80/


</VirtualHost>

